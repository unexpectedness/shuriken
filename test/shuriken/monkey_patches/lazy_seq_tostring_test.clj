(ns shuriken.monkey-patches.lazy-seq-tostring-test
  (:require [clojure.test :refer :all]
            [shuriken.monkey-patches.lazy-seq-tostring]))

; (deftest test-lazy-seq-tostring
;   (println "ici:" (.toString (lazy-seq [1 2 3])))
;   #_(is (= "#lazy()"
;          (str))))
